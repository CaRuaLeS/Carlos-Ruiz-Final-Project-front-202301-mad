export function List() {
  // Use effect with all the escaperooms
  return (
    <section className="list">
      <div>list</div>
      <ul className="list__ul">{/* { Map with each card } */}</ul>
    </section>
  );
}
